# uniffi-playground

Playing around with Mozilla's Uniffi framework

## Motivation

As a learning exercise, I want to learn about using Uniffi to create language bindings for a Rust core.
I'm particularly interested in Python and Golang bindings.

## Examples

The `examples` directory contains examples of increasing complexity

1. 000-math: a simple binding, currently only for Python of a mathlib package
